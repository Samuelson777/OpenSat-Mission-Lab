# OpenSat Mission Lab GitHub Pages Site

This directory is a framework-free static website. It can be opened locally or deployed directly to GitHub Pages.

```bash
python -m http.server 8000 --directory site
```

The repository workflow `.github/workflows/pages.yml` deploys this directory with the official GitHub Pages actions.
